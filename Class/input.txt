class CorrectProgram {
    int x;
    int y;
    int arr[]; // Declaración de arreglo sin tamaño

    void main() {
        x = 5;
        y = 10;
        arr = new int[5]; // Asignación de tamaño al arreglo
        arr[0] = x + y;
        print(arr[0]);
    }

    int sum(int a, int b) {
        return a + b;
    }
}
