class Program {
    /* Variables globales */
    int x;
    int x;                  /* Error: Identificador duplicado */
    boolean flag;
    int numbers[];
    boolean flags[];
    int invalidArray[];
    
    void main() {
        /* Error: Variables usadas antes de declaración */
        undeclaredVar = 10;
        z = y + 1;

        /* Error: Arrays con tamaño inválido */
        numbers = new int[-5];
        invalidArray = new int[0];

        /* Error: Operaciones con tipos incorrectos */
        flag = flag + 1;           /* Error: operador aritmético con boolean */
        x = true + false;          /* Error: operador aritmético con boolean */
        flag = 42 && true;         /* Error: operador lógico con int */
        flag = false || 5;         /* Error: operador lógico con int */
        
        /* Error: Comparaciones de tipos incompatibles */
        flag = (x == flag);        /* Error: comparación int con boolean */
        flag = (numbers == flags); /* Error: comparación de arrays de diferentes tipos */
        
        /* Error: Asignaciones de tipos incompatibles */
        x = flag;                  /* Error: asignación de boolean a int */
        flag = 42;                 /* Error: asignación de int a boolean */
        numbers = flags;           /* Error: asignación entre arrays de diferentes tipos */
        
        /* Error: Operaciones inválidas con arrays */
        x[0] = 5;                  /* Error: x no es array */
        numbers[true] = 1;         /* Error: índice no es int */
        numbers[flag] = 2;         /* Error: índice no es int */
        x = numbers + 1;           /* Error: operación aritmética con array */
        
        /* Error: Condiciones no booleanas */
        if (42) {                  /* Error: condición debe ser boolean */
            x = 1;
        }
        
        if (numbers) {             /* Error: condición debe ser boolean */
            x = 2;
        }
        
        /* Error: Operaciones inválidas */
        flag += 1;                 /* Error: incremento en boolean */
        numbers += 1;              /* Error: incremento en array */
        
        /* Error: Break/Continue fuera de loop */
        break;                     /* Error: break fuera de loop */
        continue;                  /* Error: continue fuera de loop */
        
        /* Error: For loop con tipos incorrectos */
        for (int i = 0; i < 10; i = i + 1) {
            if (true) {
                break;             /* Correcto: break dentro de loop */
            }
        }
        
        /* Error: Return en método void */
        return 42;                 /* Error: return con valor en método void */
    }

    int testMethod(int p1, boolean p2) {
        return true;               /* Error: tipo de retorno incorrecto */
    }

    void voidTest() {
        return 5;                  /* Error: return con valor en método void */
    }

    boolean wrongReturnType() {
        return 42;                 /* Error: tipo de retorno incorrecto */
    }

    int missingReturn(int x) {
        if (x > 0) {
            return x;
        }
        /* Error: no todas las rutas retornan valor */
    }

    void methodCallErrors() {
        testMethod();              /* Error: faltan argumentos */
        testMethod(1, false, 42);  /* Error: demasiados argumentos */
        testMethod(true, 42);      /* Error: tipos de argumentos incorrectos */
        undefinedMethod();         /* Error: método no declarado */
        x = voidTest();            /* Error: método void usado como expresión */
    }
}